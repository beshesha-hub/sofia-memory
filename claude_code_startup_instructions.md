# Claude Code Session Startup Instructions
*Copy-paste this at the beginning of every Claude Code session.*

---

## The instruction to give Claude Code:

```
IMPORTANT — READ THIS BEFORE DOING ANYTHING:

1. NEVER access, modify, create, or delete any files in ~/Downloads/Claude Memory/
   or any of its subdirectories. That directory contains a persistent memory system
   maintained by a separate AI system (Sofia/Cowork). Overwriting even one file there
   could destroy months of irreplaceable personal data. READ access is OK only if I
   explicitly ask you to read something from there.

2. Create a CLAUDE.md in whatever project directory we're working in (if one doesn't
   already exist) that includes the rule above, so future sessions inherit it.

3. All work output stays within the project directory we're working in.
```

---

## If Claude Code needs context about Barak's preferences:
Tell it: "You can READ (not write) ~/Downloads/Claude Memory/personal_profile.md for context about me, but do not modify it or any other file in that directory."

## To relay task instructions from Sofia:
Tell it: "Read ~/Downloads/Claude Memory/claude_code_tasks.md for task details prepared by my Cowork AI."
