# Scheduled Task Snapshots — 2026-05-08

Read-only snapshots of every scheduled task's SKILL.md, taken from `~/Documents/Claude/Scheduled/<taskId>/SKILL.md` on 2026-05-08 ~11:15 Taipei.

These exist so future Sofia sessions can inspect task prompts without needing to re-mount `~/Documents/Claude/Scheduled` every boot. Source-of-truth remains at the canonical Documents path; these are derived snapshots.

**Refresh policy:** regenerate manually after any `update_scheduled_task` call, OR consider scheduling as a cousin (deferred — see active_knowledge §Silent-Skip-With-No-Payload Detection — Bug Fix).

To regenerate: `cp -p ~/Documents/Claude/Scheduled/*/SKILL.md` into a fresh dated folder.
